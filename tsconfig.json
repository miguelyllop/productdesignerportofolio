{
  "compilerOptions": {
    /* Projects */
    // "incremental": true,                              /* Save .tsbuildinfo files to allow for incremental compilation of projects. */

    /* Language and Environment */
    "target": "es2016",                                  /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */
    "lib": ["dom", "dom.iterable", "esnext"],            /* Specify libraries to include in the compilation. */
    "jsx": "react-jsx",                                  /* Specify what JSX code is generated. Use "react-jsx" for React 17+. */
    "moduleResolution": "node",                          /* Use Node.js module resolution. */
    "module": "esnext",                                  /* Use ESNext modules for compatibility with modern bundlers like Webpack or Vite. */
    "esModuleInterop": true,                             /* Emit additional JavaScript to ease support for importing CommonJS modules. */
    "allowSyntheticDefaultImports": true,                /* Allow 'import x from y' when a module doesn't have a default export. */
    "strict": true,                                      /* Enable all strict type-checking options. */
    "forceConsistentCasingInFileNames": true,            /* Ensure consistent casing in imports. */
    "resolveJsonModule": true,                           /* Allow importing JSON files as modules. */
    "skipLibCheck": true,                                /* Skip type checking for all declaration files. */

    /* JavaScript Support */
    "allowJs": true,                                     /* Allow JavaScript files to be part of the program. */
    "checkJs": false,                                    /* Disable error reporting in JavaScript files. */

    /* Type Checking */
    "noImplicitAny": true,                               /* Enable error reporting for implicit 'any' types. */
    "strictNullChecks": true,                            /* Ensure strict null checking. */
    "noUnusedLocals": true,                              /* Enable error reporting when local variables aren't used. */
    "noUnusedParameters": true,                          /* Enable error reporting for unused parameters. */

    /* Emit */
    "outDir": "./dist",                                  /* Specify the output folder for all emitted files. */
    "sourceMap": true,                                   /* Create source map files for emitted JavaScript files. */

    /* Module Resolution */
    "baseUrl": ".",                                      /* Specify the base directory for non-relative module imports. */
    "paths": {                                           /* Map import paths for cleaner imports. */
      "@components/*": ["src/components/*"],
      "@styles/*": ["src/styles/*"]
    }
  },
  "include": ["src/**/*"],                               /* Include all files in the 'src' directory. */
  "exclude": ["node_modules", "dist", "build"]           /* Exclude unnecessary directories. */
}
